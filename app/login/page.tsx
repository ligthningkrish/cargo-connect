"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { ShipWheel } from "lucide-react";
import type { Role } from "@/lib/types";
export default function Login(){const router=useRouter();const [role,setRole]=useState<Role>("trader");return <main className="grid min-h-screen place-items-center bg-gradient-to-br from-navy to-[#0a4775] p-5"><div className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><Link href="/" className="flex items-center gap-2 font-bold text-navy"><span className="grid h-9 w-9 place-items-center rounded-xl bg-navy text-white"><ShipWheel size={20}/></span> CargoConnect</Link><h1 className="mt-8 text-3xl font-bold text-navy">Welcome back</h1><p className="mt-2 text-sm text-slate-500">Choose a demo role to enter your workspace.</p><div className="mt-6 grid grid-cols-3 gap-2">{(["trader","provider","admin"] as Role[]).map(r=><button key={r} onClick={()=>setRole(r)} className={`rounded-xl py-2 text-sm font-bold capitalize ${role===r?"bg-navy text-white":"bg-slate-100 text-slate-500"}`}>{r}</button>)}</div><label className="mt-6 block text-sm font-semibold">Email<input defaultValue="you@company.com" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-sky"/></label><label className="mt-4 block text-sm font-semibold">Password<input type="password" defaultValue="password" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-sky"/></label><button onClick={()=>router.push(`/dashboard/${role}`)} className="mt-6 w-full rounded-xl bg-orange py-3.5 text-sm font-bold text-white">Sign in as {role}</button><p className="mt-5 text-center text-sm text-slate-500">New to CargoConnect? <span className="font-bold text-sky">Create an account</span></p></div></main>}
