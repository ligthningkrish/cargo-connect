"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { ShipWheel } from "lucide-react";
import type { Role } from "@/lib/types";
import { getSupabaseBrowserClient } from "@/lib/supabase/browser";
export default function Login(){const router=useRouter();const [role,setRole]=useState<Role>("trader");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [name,setName]=useState("");const [signup,setSignup]=useState(false);const [status,setStatus]=useState("");const [loading,setLoading]=useState(false);const submit=async()=>{setLoading(true);setStatus("");try{const supabase=getSupabaseBrowserClient();if(signup){const {error}=await supabase.auth.signUp({email,password,options:{data:{full_name:name,role}}});if(error)throw error;setStatus("Account created. Check your email to confirm your address, then sign in.")}else{const {data,error}=await supabase.auth.signInWithPassword({email,password});if(error)throw error;const {data:profile}=await supabase.from("profiles").select("role").eq("id",data.user.id).maybeSingle();const savedRole=data.user.user_metadata?.role;const destination=profile?.role??(savedRole==="provider"||savedRole==="admin"||savedRole==="trader"?savedRole:role);router.push(`/dashboard/${destination}`)}}catch(error){setStatus(error instanceof Error?error.message:"Unable to continue. Check your Supabase setup.")}finally{setLoading(false)}};const inputClass="mt-2 w-full rounded-xl border-2 border-slate-300 bg-white px-4 py-3 font-semibold text-black placeholder:font-medium placeholder:text-slate-600 outline-sky";return <main className="grid min-h-screen place-items-center bg-gradient-to-br from-navy to-[#0a4775] p-5"><div className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><Link href="/" className="flex items-center gap-2 font-bold text-navy"><span className="grid h-9 w-9 place-items-center rounded-xl bg-navy text-white"><ShipWheel size={20}/></span> CargoConnect</Link><h1 className="mt-8 text-3xl font-bold text-black">{signup?"Create your account":"Welcome back"}</h1><p className="mt-2 text-sm font-semibold text-slate-700">{signup?"Choose your workspace and create secure credentials.":"Sign in to continue to your workspace."}</p>{signup&&<label className="mt-5 block text-sm font-bold text-black">Full name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" className={inputClass}/></label>}<div className="mt-6 grid grid-cols-3 gap-2">{(["trader","provider","admin"] as Role[]).map(r=><button key={r} disabled={signup&&r==="admin"} onClick={()=>setRole(r)} className={`rounded-xl py-2 text-sm font-bold capitalize ${role===r?"bg-navy text-white":"border border-slate-300 bg-slate-100 text-black hover:bg-slate-200"} disabled:cursor-not-allowed disabled:opacity-40`}>{r}</button>)}</div><label className="mt-6 block text-sm font-bold text-black">Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@company.com" className={inputClass}/></label><label className="mt-4 block text-sm font-bold text-black">Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="At least 6 characters" className={inputClass}/></label>{status&&<p className="mt-4 rounded-xl bg-sky/10 p-3 text-sm font-semibold text-black">{status}</p>}<button disabled={loading} onClick={submit} className="mt-6 w-full rounded-xl bg-orange py-3.5 text-sm font-bold text-white shadow-md transition hover:scale-[1.01] disabled:opacity-60">{loading?"Please wait…":signup?`Create ${role} account`:`Sign in as ${role}`}</button><button onClick={()=>{setSignup(!signup);setStatus("")}} className="mt-5 w-full text-center text-sm font-semibold text-black">{signup?"Already have an account? ":"New to CargoConnect? "}<span className="font-bold text-sky underline underline-offset-2">{signup?"Sign in":"Create an account"}</span></button></div></main>}
