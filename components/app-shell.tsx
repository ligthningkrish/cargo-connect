"use client";
import Link from "next/link";
import { Bell, LayoutDashboard, PackageOpen, ShipWheel, UserRound, MessageCircle, LogOut } from "lucide-react";
import type { Role } from "@/lib/types";
const nav = [{label:"Overview",icon:LayoutDashboard},{label:"Containers",icon:PackageOpen},{label:"Bookings",icon:ShipWheel},{label:"Messages",icon:MessageCircle}];
export function AppShell({ role, children }: {role:Role; children:React.ReactNode}) { const label = role === "provider" ? "Provider Portal" : role === "admin" ? "Admin Console" : "Trader Dashboard"; return <div className="min-h-screen bg-[#f5f9fc] text-ink">
 <aside className="fixed inset-y-0 hidden w-64 flex-col border-r border-slate-200 bg-white px-5 py-7 lg:flex"><Link href="/" className="mb-12 flex items-center gap-3 font-bold text-navy"><span className="grid h-10 w-10 place-items-center rounded-xl bg-navy text-white"><ShipWheel size={22}/></span><span>Cargo<span className="text-sky">Connect</span></span></Link><p className="mb-3 px-3 text-[10px] font-bold uppercase tracking-[.18em] text-slate-400">{label}</p>{nav.map((n,i)=><button key={n.label} className={`mb-1 flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium ${i===0?"bg-sky/10 text-navy":"text-slate-500 hover:bg-slate-50"}`}><n.icon size={18}/>{n.label}</button>)}<div className="mt-auto"><div className="mb-5 flex items-center gap-3 rounded-xl bg-slate-50 p-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-orange text-xs font-bold text-white">{role[0].toUpperCase()}</span><div><p className="text-sm font-semibold capitalize">{role} user</p><p className="text-xs text-emerald-600">● Online</p></div></div><Link href="/" className="flex items-center gap-2 px-3 py-2 text-sm text-slate-500"><LogOut size={16}/> Sign out</Link></div></aside>
 <main className="lg:ml-64"><header className="flex h-20 items-center justify-between border-b border-slate-200 bg-white px-6 md:px-10"><div><p className="text-xs font-medium text-slate-400">Welcome back</p><h1 className="font-bold text-navy">{label}</h1></div><div className="flex items-center gap-4"><button className="relative rounded-full p-2 text-slate-500 hover:bg-slate-100"><Bell size={19}/><i className="absolute right-2 top-2 h-2 w-2 rounded-full bg-orange"/></button><span className="grid h-9 w-9 place-items-center rounded-full bg-navy text-white"><UserRound size={17}/></span></div></header><div className="p-6 md:p-10">{children}</div></main></div> }
