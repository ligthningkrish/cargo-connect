"use client";
import { useMemo, useState, type ComponentType } from "react";
import { AnimatePresence, motion } from "framer-motion";
import { CalendarDays, ChevronRight, CircleDollarSign, Mail, MapPin, MessageCircle, Phone, Plus, Search, TrendingUp, Users, X } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { CargoGrid } from "@/components/cargo-grid";
import { CargoShip } from "@/components/ship";
import { containers, type Container, type Role } from "@/lib/types";

function Stat({label,value,icon:Icon,detail}:{label:string;value:string;icon:ComponentType<{size?:number}>;detail:string}) { return <div className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><div className="flex justify-between"><span className="text-sm text-slate-500">{label}</span><span className="rounded-xl bg-sky/10 p-2 text-sky"><Icon size={18}/></span></div><p className="mt-5 text-2xl font-bold text-navy">{value}</p><p className="mt-1 text-xs text-emerald-600">{detail}</p></div> }
function ContainerCard({container,onBook}:{container:Container;onBook:(c:Container)=>void}) { return <div className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><div className="flex justify-between"><div><p className="text-xs font-bold text-sky">{container.id}</p><h3 className="mt-1 font-bold text-navy">{container.route}</h3></div>{container.verified && <span className="h-fit rounded-full bg-emerald-50 px-2 py-1 text-[10px] font-bold text-emerald-700">VERIFIED</span>}</div><div className="mt-4 flex gap-4 text-xs text-slate-500"><span className="flex items-center gap-1"><CalendarDays size={13}/>{container.departure}</span><span className="flex items-center gap-1"><CircleDollarSign size={13}/> ${container.price}/box</span></div><div className="mt-5"><CargoGrid occupied={container.occupied} compact/></div><button onClick={()=>onBook(container)} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-navy py-3 text-sm font-bold text-white hover:bg-[#0a4775]">Book space <ChevronRight size={16}/></button></div> }
function BookingModal({container,onClose}:{container:Container;onClose:()=>void}) { const [boxes,setBoxes]=useState(1); const [done,setDone]=useState(false); return <div className="fixed inset-0 z-50 grid place-items-center bg-navy/50 p-5 backdrop-blur-sm"><motion.div initial={{opacity:0,scale:.94}} animate={{opacity:1,scale:1}} className="relative w-full max-w-lg overflow-hidden rounded-3xl bg-white p-7 shadow-2xl"><button onClick={onClose} className="absolute right-5 top-5 text-slate-400"><X/></button>{!done ? <><p className="text-sm font-bold text-sky">BOOK CARGO SPACE</p><h2 className="mt-1 text-2xl font-bold text-navy">{container.route}</h2><p className="mt-2 text-sm text-slate-500">{container.company} · {container.departure}</p><div className="my-6 rounded-2xl bg-slate-50 p-4"><CargoGrid occupied={container.occupied + boxes} compact/></div><div className="flex items-center justify-between"><div><p className="text-sm font-semibold">How many boxes?</p><p className="text-xs text-slate-500">${container.price} each · {100-container.occupied} still available</p></div><div className="flex items-center gap-3 rounded-xl border border-slate-200 p-1"><button onClick={()=>setBoxes(Math.max(1,boxes-1))} className="px-3 text-lg">−</button><b className="w-5 text-center">{boxes}</b><button onClick={()=>setBoxes(Math.min(100-container.occupied,boxes+1))} className="px-3 text-lg">+</button></div></div><button onClick={()=>setDone(true)} className="mt-6 w-full rounded-xl bg-orange py-3.5 text-sm font-bold text-white">Confirm & pay ${boxes*container.price}</button></> : <div className="py-3 text-center"><p className="text-sm font-bold text-emerald-600">PAYMENT CONFIRMED</p><h2 className="mt-2 text-3xl font-bold text-navy">Your cargo is booked!</h2><p className="mx-auto mt-2 max-w-sm text-sm text-slate-500">Your {boxes} box{boxes>1?"es":""} are now on their way to the ship.</p><div className="my-8 flex h-44 items-end justify-center overflow-hidden rounded-2xl bg-gradient-to-b from-[#e1f5ff] to-[#86cbef]"><CargoShip sailing/></div><div className="rounded-2xl bg-sky/10 p-4 text-left"><p className="text-xs font-bold text-sky">PROVIDER CONTACT UNLOCKED</p><p className="mt-2 font-bold text-navy">BlueWave Logistics</p><p className="mt-1 flex items-center gap-2 text-sm text-slate-600"><Phone size={14}/> +91 22 4378 2100 <Mail size={14} className="ml-2"/> ops@bluewave.io</p></div><button onClick={onClose} className="mt-6 w-full rounded-xl bg-navy py-3 text-sm font-bold text-white">View my booking</button></div>}</motion.div></div> }
function Trader() { const [term,setTerm]=useState(""); const [booking,setBooking]=useState<Container|null>(null); const visible=useMemo(()=>containers.filter(c=>c.route.toLowerCase().includes(term.toLowerCase())||c.origin.toLowerCase().includes(term.toLowerCase())||c.destination.toLowerCase().includes(term.toLowerCase())),[term]); return <><div className="flex flex-col justify-between gap-4 md:flex-row"><div><p className="text-sm text-slate-500">Find a route that fits your cargo.</p><h2 className="mt-1 text-3xl font-bold text-navy">Available capacity</h2></div><div className="flex items-center gap-2 rounded-xl border border-slate-200 bg-white px-3 shadow-sm"><Search size={18} className="text-slate-400"/><input value={term} onChange={e=>setTerm(e.target.value)} placeholder="Origin, destination..." className="w-56 py-3 text-sm outline-none"/></div></div><div className="mt-7 grid gap-4 md:grid-cols-3"><Stat label="Active bookings" value="03" icon={TrendingUp} detail="2 shipments in transit"/><Stat label="Cargo booked" value="184 boxes" icon={Users} detail="↑ 12% this quarter"/><Stat label="Amount saved" value="$1,820" icon={CircleDollarSign} detail="vs. full container load"/></div><div className="mt-10 grid gap-5 lg:grid-cols-3">{visible.map(c=><ContainerCard key={c.id} container={c} onBook={setBooking}/>)}</div><AnimatePresence>{booking&&<BookingModal container={booking} onClose={()=>setBooking(null)}/>}</AnimatePresence></> }
function Provider() { const [occupied,setOccupied]=useState(34); const [adding,setAdding]=useState(false); const [notice,setNotice]=useState(""); return <><div className="flex items-start justify-between"><div><p className="text-sm text-slate-500">BlueWave Logistics · <span className="font-semibold text-orange">Pending verification</span></p><h2 className="mt-1 text-3xl font-bold text-navy">Manage your capacity</h2></div><button onClick={()=>setAdding(true)} className="flex items-center gap-2 rounded-xl bg-navy px-4 py-3 text-sm font-bold text-white"><Plus size={17}/> Add container</button></div>{notice&&<div className="mt-5 flex items-center justify-between rounded-xl bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-700">{notice}<button onClick={()=>setNotice("")}>×</button></div>}<div className="mt-7 grid gap-4 md:grid-cols-3"><Stat label="Available space" value={`${100-occupied} boxes`} icon={BoxesIcon} detail="Across 2 active routes"/><Stat label="Revenue this month" value="$8,470" icon={CircleDollarSign} detail="↑ 18% from July"/><Stat label="New inquiries" value="12" icon={MessageCircle} detail="4 unread messages"/></div><div className="mt-8 grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><div className="rounded-3xl bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><p className="text-xs font-bold text-sky">CC-1024 · MV ATLANTIC STAR</p><h3 className="mt-1 text-xl font-bold text-navy">Mumbai → Rotterdam</h3></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">Accepting bookings</span></div><div className="mt-6"><CargoGrid occupied={occupied}/></div><div className="mt-5 flex gap-3"><button onClick={()=>{setOccupied(Math.min(100,occupied+1));setNotice("New booking received — cargo occupancy updated.")}} className="rounded-lg bg-sky px-4 py-2 text-sm font-bold text-white">Simulate booking +1</button><button onClick={()=>setNotice("Shipment editor opened — changes are ready to save.")} className="rounded-lg border border-slate-200 px-4 py-2 text-sm font-bold text-navy">Edit shipment</button></div></div><div className="rounded-3xl bg-navy p-6 text-white"><p className="text-sm font-bold text-sky">UPCOMING DEPARTURE</p><h3 className="mt-4 text-2xl font-bold">18 Aug 2026</h3><p className="mt-2 text-sm text-slate-300">Departure from Nhava Sheva in 29 days.</p><div className="mt-7 border-t border-white/15 pt-5 text-sm"><p className="flex items-center gap-2"><MapPin size={16} className="text-sky"/> Rotterdam, Netherlands</p></div></div></div>{adding&&<div className="fixed inset-0 z-50 grid place-items-center bg-navy/40 p-5"><div className="w-full max-w-md rounded-3xl bg-white p-7 shadow-2xl"><h3 className="text-xl font-bold text-navy">Add a container</h3><p className="mt-2 text-sm text-slate-500">Create a draft shipment for admin verification.</p><input placeholder="Origin → destination" className="mt-6 w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-sky"/><input placeholder="Price per box" className="mt-3 w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-sky"/><div className="mt-6 flex justify-end gap-3"><button onClick={()=>setAdding(false)} className="px-4 text-sm font-bold text-slate-500">Cancel</button><button onClick={()=>{setAdding(false);setNotice("Container draft created and sent for approval.")}} className="rounded-xl bg-navy px-4 py-3 text-sm font-bold text-white">Create draft</button></div></div></div>}</> }
const BoxesIcon = ({size}:{size?:number}) => <span style={{fontSize:size}}>▦</span>;
function Admin() { const [decision,setDecision]=useState<"approved"|"rejected"|null>(null); return <><div><p className="text-sm text-slate-500">Platform health at a glance.</p><h2 className="mt-1 text-3xl font-bold text-navy">Command center</h2></div><div className="mt-7 grid gap-4 md:grid-cols-4"><Stat label="Total users" value="2,846" icon={Users} detail="↑ 9.8% this month"/><Stat label="Active containers" value="128" icon={BoxesIcon} detail="24 new this week"/><Stat label="Gross bookings" value="$124k" icon={CircleDollarSign} detail="↑ 14.2% this month"/><Stat label="Pending reviews" value={decision?"0":"1"} icon={TrendingUp} detail="Provider applications"/></div><div className="mt-8 grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-3xl bg-white p-6 shadow-sm"><p className="text-sm font-bold text-navy">Provider verification queue</p><div className="mt-5 flex items-center justify-between rounded-2xl border border-slate-100 p-4"><div><p className="font-bold text-navy">BlueWave Logistics</p><p className="mt-1 text-xs text-slate-500">Submitted Jul 19 · Mumbai, India</p></div>{decision?<span className={`rounded-full px-3 py-2 text-xs font-bold ${decision==="approved"?"bg-emerald-50 text-emerald-700":"bg-rose-50 text-rose-600"}`}>{decision==="approved"?"Approved and notified":"Rejected with feedback"}</span>:<div className="flex gap-2"><button onClick={()=>setDecision("approved")} className="rounded-lg bg-navy px-3 py-2 text-xs font-bold text-white">Approve</button><button onClick={()=>setDecision("rejected")} className="rounded-lg bg-rose-50 px-3 py-2 text-xs font-bold text-rose-600">Reject</button></div>}</div></div><div className="rounded-3xl bg-gradient-to-br from-sky to-[#1979b3] p-6 text-white"><p className="text-sm font-bold text-white/70">OCCUPANCY RATE</p><p className="mt-3 text-5xl font-bold">71.4%</p><p className="mt-2 text-sm text-white/80">Network capacity is performing 8% above target.</p></div></div></> }
export function Dashboard({role}:{role:Role}) { return <AppShell role={role}>{role==="trader"?<Trader/>:role==="provider"?<Provider/>:<Admin/>}</AppShell> }
